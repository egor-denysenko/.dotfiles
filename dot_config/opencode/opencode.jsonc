{
  "$schema": "https://opencode.ai/config.json",

  // LSP servers — pyrefly for Python, built-in auto-install for the rest
  "lsp": {
    "pyright": { "disabled": true },
    "pyrefly": {
      "command": ["pyrefly"],
      "extensions": ["py"]
    }
  }
}
