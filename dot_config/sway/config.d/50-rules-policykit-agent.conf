# Vendored from Fedora /usr/share/sway/config.d/50-rules-policykit-agent.conf — edit here, re-diff against the original when re-vendoring.
for_window [app_id="lxqt-policykit-agent"] {
    floating enable
    move position center
}
