# Vendored from Fedora /usr/share/sway/config.d/90-bar.conf — edit here, re-diff against the original when re-vendoring.
# Status Bar: waybar
#
# Read `man 5 sway-bar` for more information about this section.
# Read `man 5 waybar` for more information about the waybar instance style and layout configuration
#
# Requires: waybar

bar {
    swaybar_command waybar
}
