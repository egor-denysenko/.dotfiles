# Vendored from Fedora /usr/share/sway/config.d/65-mode-passthrough.conf — edit here, re-diff against the original when re-vendoring.
# A special mode for passing any keybindings to the focused application

mode "passthrough" {
    bindsym $mod+Pause mode default
}
bindsym $mod+Pause mode "passthrough"
